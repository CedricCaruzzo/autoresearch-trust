# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
worktrees/
results/
queue/

# Agent prompt files (generated per-session by launchers)
CLAUDE.md
AGENTS.md

# Experimental code/artifacts
dev/

# Results file
results.tsv

# Trust layer — key is a local secret, NEVER commit it.
# The manifest (.trust_manifest.json) IS committed — that's the proof.
.trust_key

# Trust runtime DB (generated locally)
trust.db
